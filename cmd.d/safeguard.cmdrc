#!/bin/bash

# CHECK ENV
# MAKE SURE ALIASES ARENT EXPORTED TO PRODUCTION SERVERS ON ACCIDENT

# if [ $WORKING_ENV == "local" ]; then
  # overwrite and include some basic safety mechanisms for dangerous commands
  # unalias "";
  # alias = ""
# fi
